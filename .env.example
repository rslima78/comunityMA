# Desenvolvimento com o Postgres do docker-compose (npm run db:up):
DATABASE_URL=postgresql://communityma:communityma@localhost:5433/communityma

# Producao: cole a DATABASE_URL que o Railway gera no plugin Postgres.
# DATABASE_URL=postgresql://postgres:SENHA@HOST.proxy.rlwy.net:PORTA/railway

# Admin inicial (usado pelo seed na Etapa 4). Nao commitar valores reais.
ADMIN_USER=
ADMIN_PASSWORD=
