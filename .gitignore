# dependencias e build
/node_modules
/.next
/out
next-env.d.ts
*.tsbuildinfo

# variaveis de ambiente
.env
.env.*
!.env.example

# DADOS REAIS DE ALUNOS -- nunca versionar.
# Planilhas com nome, CPF, nascimento, filiacao, notas e ocorrencias
# de ~890 menores. Uma vez no historico do git, nao sai mais.
*.xlsx
*.xls
*.csv
*.sql.gz
*.dump

# sistema
.DS_Store
Thumbs.db

# relatorios de importacao contem nomes de alunos
/relatorios
